{
  "name": "verifone-edge-relay",
  "version": "1.0.0",
  "type": "module",
  "description": "Verifone Commander edge relay — syncs LAN POS data to Shre AI cloud",
  "main": "src/main.mjs",
  "scripts": {
    "start": "node src/main.mjs",
    "build": "node build/build.mjs",
    "test": "node --test src/**/*.test.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "fast-xml-parser": "^4.4.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
